{
  "name": "p2pflow-smart-contracts",
  "version": "1.0.0",
  "description": "P2PFlow Diamond Proxy Smart Contracts (EIP-2535)",
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": "24.18.0",
    "npm": "11.16.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:invariant-gas": "hardhat test test/v2-invariant-gas.test.js",
    "test:system": "node test/system/local-system.mjs",
    "coverage": "hardhat coverage",
    "verify:toolchain": "node scripts/verify-phase1.js",
    "verify:phase1": "npm run verify:toolchain && npm run compile",
    "verify": "npm run verify:phase1 && npm run protocol:generated:check && npm test && npm run protocol:check && npm run preflight:fixture",
    "verify:workspace:phase1": "node scripts/verify-workspace-phase1.js",
    "verify:workspace": "npm run verify:workspace:phase1 && npm run protocol:vendor:check && node scripts/verify-protocol-consumers.mjs && npm run verify:executor:phase3 && npm run verify:image-privacy",
    "verify:image-privacy": "node scripts/verify-workspace-image-privacy.mjs",
    "verify:executor:phase3": "node scripts/verify-executor-phase3.mjs",
    "protocol:install": "npm --prefix packages/protocol ci --no-audit --no-fund",
    "protocol:fixture": "npm run compile && node scripts/generate-protocol-fixture.js",
    "protocol:fixture:check": "npm run compile && node scripts/generate-protocol-fixture.js --check",
    "protocol:generated:check": "node scripts/generate-protocol-fixture.js --check && npm --prefix packages/protocol run generate:check",
    "protocol:generate": "npm run protocol:fixture && npm --prefix packages/protocol run generate",
    "protocol:build": "npm run protocol:fixture && npm --prefix packages/protocol run build",
    "protocol:test": "npm run protocol:fixture && npm --prefix packages/protocol test",
    "protocol:pack": "npm run protocol:fixture && npm --prefix packages/protocol run pack:artifact",
    "protocol:vendor": "node scripts/vendor-protocol.mjs",
    "protocol:vendor:check": "node scripts/vendor-protocol.mjs --check",
    "protocol:check": "npm run protocol:fixture:check && npm --prefix packages/protocol run check",
    "preflight:fixture": "npm --prefix packages/protocol run build --silent && node scripts/preflight-v2.mjs --check-local-fixture",
    "preflight:base-sepolia": "npm --prefix packages/protocol run build --silent && node scripts/preflight-v2.mjs",
    "deploy": "node scripts/deployment-disabled.mjs",
    "upgrade": "node scripts/deployment-disabled.mjs",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "playwright": "1.62.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
