{
  "name": "p2pflow-smart-contracts",
  "version": "1.0.0",
  "description": "P2PFlow Diamond Proxy Smart Contracts (EIP-2535)",
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": "24.18.0",
    "npm": "11.16.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "verify:toolchain": "node scripts/verify-phase1.js",
    "verify:phase1": "npm run verify:toolchain && npm run compile",
    "verify": "npm run verify:phase1 && npm run protocol:check",
    "verify:workspace:phase1": "node scripts/verify-workspace-phase1.js",
    "verify:workspace": "npm run verify:workspace:phase1 && npm run protocol:vendor:check && node scripts/verify-protocol-consumers.mjs",
    "protocol:install": "npm --prefix packages/protocol ci --no-audit --no-fund",
    "protocol:fixture": "npm run compile && node scripts/generate-protocol-fixture.js",
    "protocol:generate": "npm run protocol:fixture && npm --prefix packages/protocol run generate",
    "protocol:build": "npm run protocol:fixture && npm --prefix packages/protocol run build",
    "protocol:test": "npm run protocol:fixture && npm --prefix packages/protocol test",
    "protocol:pack": "npm run protocol:fixture && npm --prefix packages/protocol run pack:artifact",
    "protocol:vendor": "node scripts/vendor-protocol.mjs",
    "protocol:vendor:check": "node scripts/vendor-protocol.mjs --check",
    "protocol:check": "npm run protocol:fixture && npm --prefix packages/protocol run check",
    "stress": "hardhat compile && node scripts/stressTest.js",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:base-sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:mock-usdc:base-sepolia": "hardhat run scripts/deployMockUsdc.js --network baseSepolia",
    "upgrade:local": "hardhat run scripts/upgrade.js --network localhost",
    "upgrade:sepolia": "hardhat run scripts/upgrade.js --network sepolia",
    "upgrade:base-sepolia": "hardhat run scripts/upgrade.js --network baseSepolia",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^17.4.2"
  }
}
